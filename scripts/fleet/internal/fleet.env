# fleet.env — minimal shared environment contract for governed helper scripts
# Keep this file policy-free: only portable defaults shared by multiple script families.

FLEET_LOCK_DIR="${FLEET_LOCK_DIR:-${TMPDIR:-/tmp}/openclaw-fleet-locks}"
